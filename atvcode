#include<stdio.h>
int main(){
    long long n, a, b;
    scanf("%lld", &n);
    scanf("%lld %lld", &a, &b);
    if (n < 1 || a < 1 || b < 1){
        return 0;
    }
    else if (n > 10000000 || a > 10000000 || b > 10000000){
        return 0;

    }

    return 0;
}